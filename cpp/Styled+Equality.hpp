#pragma once

#include "Styled.hpp"

namespace margelo::nitro::cssnitro {
    inline bool operator==(const Styled &lhs, const Styled &rhs) {
        return false;
    }
} // namespace margelo::nitro::cssnitro
